import React from "react";
import ReactDOM from "react-dom/client";
import LocationPage from "./templates/location";
import "./styles/index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <LocationPage />
  </React.StrictMode>
);
